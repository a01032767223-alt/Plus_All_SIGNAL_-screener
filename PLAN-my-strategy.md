# "내 전략" 탭 기획서 — 사용자 맞춤 가중치 스크리닝

> 대상: signal-screener (매수신호 · 추세전환에 이은 세 번째 스코어링 모드)
> 상태: **Phase 0~3 구현 완료** (Phase 4 백테스트는 다음 기획)
> 작성 기준일: 2026-08-16 · 구현 반영 2026-08-17

## 구현 결과 요약 (문서 대비 달라진 점)

| 항목 | 기획 | 실제 |
|---|---|---|
| 매트릭스 파일 크기 (국내 1,800종목) | 200 KB / gzip 77 KB | **204 KB / gzip 74 KB** — 예산 안 |
| 재계산 정확도 | 오차 < 0.1점 | **최대 0.027점**, 순서까지 일치 |
| 재계산 속도 | "슬라이더마다 재계산해도 무해" | **1.9 ms**/1,800종목 (측정) |
| 슬라이더 → 화면 반영 | rAF 1회로 합침 | 초기 93 ms → **23 ms** (드래그 중 카드 20장으로 축소하는 로직 추가) |
| 추세전환 커스텀 | Phase 3 | 함께 구현. 주식에서는 코인 특화 슬라이더를 숨김 |
| 코드량 | index.html +450줄 예상 | 실제 **+약 700줄** (1,000 → 약 1,700줄) |

기획에 없다가 구현 중 추가한 것:

- **드래그 중 렌더 축소** — 손을 끄는 동안 카드 20장, 입력이 멎고 160ms 뒤 100장 복원.
  기획 단계에서 "rAF로 합치면 충분"이라고 봤지만 실측 93ms로 체감이 끊겨 추가했다.
- **CI 테스트 누락 수정** — `tests.yml`이 테스트 파일을 하나씩 나열해 돌리고 있어
  pytest 형식으로 쓴 `test_reversal.py`가 **초록불인 채 한 번도 실행되지 않고 있었다.**
  `python -m pytest tests -q`로 바꿨다.
- **저작권·이용 조건 가이드 섹션** — 추세전환에만 붙어 있던 한 줄을 앱 전체 기준으로 확장.
- **`tests/e2e/`** — 브라우저 점검 스크립트 6종을 리포에 남김(CI 미포함).

§13의 확인 사항 처리:

1. 국내 유니버스 실제 규모 → 실측은 실제 실행 후에만 가능. 1,800종목 가정으로
   부풀린 데이터에서 성능·크기를 검증해 두었다(`tests/e2e/make_big_matrix.py`).
2. `sw.js` 캐시 버전 → **`screener-v6` → `screener-v7`로 상승.** 주석이 "미국주식 탭"에
   머물러 있었으므로 추세전환 배포분도 반영되지 않았을 가능성이 크다.
3. 프리셋 이름 → 각 프리셋에 한 줄 설명(`desc`)을 붙여 버튼 아래에 표시.
4. 필터 기본값 → 전부 "제한 없음". 사용자가 켠 조건만 걸리게 했다.

---

## 0. 한 장 요약

지금 앱은 **개발자가 정한 비중**(이동평균 25 · 거래량 20 · 지지저항 20 · RSI 10 · MACD 8 · 볼린저 7 · ADX 5 · OBV 5)으로만 점수를 매긴다. 이 비중은 합리적인 기본값이지만, 사용자마다 신뢰하는 지표가 다르다. 거래량과 OBV만 보는 사람에게 지금 화면의 1위는 남의 1위다.

**"내 전략" 탭은 이 비중을 사용자가 직접 조절하게 하고, 전 종목을 그 비중으로 즉시 다시 줄 세운다.**

핵심 설계 결정 네 가지:

| 결정 | 내용 | 이유 |
|---|---|---|
| **재계산은 브라우저에서** | 서버는 지표별 원점수만 내려주고, 총점 합산은 JS가 한다 | 슬라이더를 움직일 때마다 GitHub Actions를 돌릴 수는 없다. 실측 재계산 시간 **0.71ms/1,800종목** |
| **전 종목 매트릭스를 별도 파일로** | `{market}_matrix.json`을 지연 로드 | 지금 목록은 55점 이상 상위 200종목뿐이다. 내 비중으로 90점이 될 종목이 그 밖에 있으면 이 기능은 무의미해진다 |
| **등급(S·A·B·C) 대신 순위(#1)** | 커스텀 결과에는 등급 배지를 쓰지 않는다 | 등급은 "기본 비중 기준"이라는 의미를 갖는다. 재계산 점수에 S를 붙이면 사용자가 스스로 만든 S등급을 신뢰하게 된다 |
| **기본 점수를 항상 병기** | 카드마다 "기본 71.3 · 순위 ▲38" | 내 설정이 기본과 얼마나 벌어졌는지 매 순간 보이게 한다. 과최적화 방지 장치 |

**검증 완료 (기획 단계에서 이미 확인함):**

```
검증 40종목 · 정렬보너스 발생 10건 · 최대 오차 0.0265점
→ 기본 가중치로 재계산한 값 = 서버가 계산한 총점 (불변식 성립)
```

즉 사용자가 프리셋을 "기본"으로 두면 내 전략 탭의 순서가 매수신호 탭과 **완전히 일치한다.** 이 불변식이 이 기능 전체의 신뢰 근거이고, 테스트로 고정한다.

---

## 1. 문제 정의

### 1-1. 지금 무엇이 아쉬운가

- 총점 산식이 블랙박스다. 가이드에 비중표는 있지만 **바꿀 수 없어서** 사용자는 "왜 이게 1위지?"를 납득하거나 포기하는 두 선택지밖에 없다.
- 스타일이 다른 사용자를 못 담는다. 돌파 매매자는 지지저항·거래량이 전부인데 현재 산식은 이동평균에 25%를 준다.
- 55점 미만 종목은 아예 안 보인다. 기본 비중에서 55점인 종목이 다른 비중에서는 상위권일 수 있는데, 그 사실을 확인할 방법이 없다.
- 추세전환 탭도 같은 문제를 갖는다. 6개 카테고리 배점(25/20/20/15/10/10)이 고정이다.

### 1-2. 하지 않을 것 (스코프 밖)

- **지표를 새로 만드는 기능** — 사용자가 수식을 쓰는 건 다른 제품이다. 조절 대상은 기존 8지표 + 추세전환 6카테고리로 한정.
- **지표 파라미터 변경** (RSI 14 → 9 등) — 서버 재계산이 필요해 브라우저에서 불가능하다.
- **자동 최적화** ("가장 성과 좋은 비중 찾아줘") — 과최적화 기계를 만드는 일이다. 명시적으로 안 만든다.
- **간이 백테스트** — 다음 단계(§12)로 미룸. 현재 히스토리 포맷으로는 계산 불가.

---

## 2. 사용자 시나리오

**A. 돌파 매매자 (고급)**
코인 탭 → 내 전략 → 프리셋 "돌파형" → 필수 조건에서 거래대금 50억 이상 → 결과 12종목. 3위 종목 카드에 `NEW 발굴` 배지. 눌러보니 기본 점수는 52점이라 매수신호 탭에는 아예 없던 종목. 상세에서 "지지저항 비중을 20→30으로 올린 덕에 +7.4점" 확인.

**B. 초보자 (프리셋만)**
가이드에서 "내 전략" 설명을 읽고 → 프리셋 6개 중 "수급 중심형"을 눌러봄 → 상단에 "기본 상위 20과 겹치는 종목 7개, 새로 올라온 종목 13개" → 두 목록의 차이를 눈으로 배움. 슬라이더는 열지도 않음.

**C. 반복 사용자**
자기 비중을 "내 조합 A"로 저장 → 매일 아침 앱을 열면 마지막 설정이 자동 복원 → 링크로 지인에게 공유(`#s=...`) → 지인 화면에 "공유받은 전략을 불러왔습니다 [내 설정으로 저장]".

---

## 3. 정보 구조 · 화면 설계

### 3-1. 탭 위치

헤더 탭(국내주식/미국주식/코인/가이드)은 **시장** 축이고, 콘텐츠 토글은 **스코어링 방식** 축이다. 내 전략은 후자이므로 콘텐츠 토글의 세 번째 버튼으로 넣는다.

```
[ 매수신호 ] [ 추세전환 ] [ 내 전략 ⚙ NEW ]
```

- `NEW` 배지는 추세전환에서 떼어 내 전략으로 옮긴다.
- 360px 화면에서 3버튼 = 버튼당 약 110px. `.ctbtn` 폰트를 12.5px로 한 단계 낮추는 미디어쿼리만 추가하면 된다(이미 `.tab`에 같은 패턴이 있다).
- 시장 탭을 바꿔도 선택은 유지된다(현재 추세전환과 동일 동작, E2E로 이미 보장 중).

### 3-2. 화면 구성

```
┌ 전략 ─────────────────────────────────────┐
│ 기준  (•) 매수신호   ( ) 추세전환             │  ← Phase 3에서 활성화
│                                            │
│ [기본] [추세추종] [돌파] [눌림목]              │
│ [수급] [모멘텀] [균형] [내 조합 A ▾]           │
│                                            │
│ ▸ 직접 조절                                  │  ← 접힘 상태가 기본
└────────────────────────────────────────┘
   펼치면 ↓
   이동평균선   ━━━━━━━●━━━  25   [−][+]
   거래량       ━━━━━●━━━━━  20
   지지·저항     ━━━━━●━━━━━  20
   RSI          ━━●━━━━━━━  10
   MACD         ━●━━━━━━━━   8
   볼린저밴드     ━●━━━━━━━━   7
   ADX          ●━━━━━━━━━   5
   OBV          ●━━━━━━━━━   5
   ───────────────────────────────
   합계 100  ·  [기본으로 되돌리기]  [이 조합 저장]

┌ 필수 조건 ▸ ──────────────────────────────┐
│ 거래대금 최소   [10억 ▾]                     │
│ 손익비 최소     [2.0 이상 ▾]                 │
│ 경고 신호       [0건만 ▾]                    │
│ 당일 등락률     [+15% 이하 ▾]                │
│ 하루 변동폭(ATR) [제한 없음 ▾]                │
└────────────────────────────────────────┘

[내 점수 84+ 9] [새로 발굴 13] [기본과 겹침 7] [필터 제외 41] [스캔 1,812]

최소 점수 ━━━━●━━━  70      정렬 [내 점수순 ▾]   [검색            ]

┌──────────────────────────────────────┐
│ #1  삼성전자              72,300  ▲ 1.4%   │
│     내 점수 88.4  ·  기본 71.3 (▲38위)      │
│     ██████████████████░░                  │
│     거래량 +21.2  OBV +14.8  지지저항 +12.1  │  ← 내 비중 기여도 상위 3
│     손익비 2.8 · 거래대금 3,120억 · 경고 없음  │
└──────────────────────────────────────┘
```

### 3-3. 프리셋 정의 (합계 100)

| 프리셋 | 이동평균 | 거래량 | 지지저항 | RSI | MACD | 볼린저 | ADX | OBV | 한 줄 설명 |
|---|--:|--:|--:|--:|--:|--:|--:|--:|---|
| **기본(교과서)** | 25 | 20 | 20 | 10 | 8 | 7 | 5 | 5 | 매수신호 탭과 동일 |
| **추세 추종형** | 35 | 10 | 15 | 5 | 10 | 3 | 17 | 5 | 이미 방향이 잡힌 종목 위주 |
| **돌파형** | 20 | 28 | 30 | 3 | 7 | 5 | 5 | 2 | 저항 돌파 + 거래량 동반 |
| **눌림목형** | 22 | 8 | 18 | 28 | 6 | 13 | 2 | 3 | 상승 추세 중 과매도 되돌림 |
| **수급 중심형** | 15 | 33 | 12 | 5 | 5 | 3 | 5 | 22 | 거래량·OBV가 먼저 움직인 종목 |
| **모멘텀형** | 15 | 12 | 10 | 20 | 25 | 5 | 10 | 3 | MACD·RSI 전환 시점 |
| **균형형** | 13 | 13 | 12 | 12 | 12 | 12 | 13 | 13 | 특정 지표 편향 제거 |

추세전환용 프리셋(6카테고리, 합계 100):

| 프리셋 | 가격구조 | 거래량 | 모멘텀 | 이동평균 | 변동성 | 코인특화 |
|---|--:|--:|--:|--:|--:|--:|
| 기본 | 25 | 20 | 20 | 15 | 10 | 10 |
| 구조 우선형 | 40 | 15 | 15 | 15 | 10 | 5 |
| 다이버전스형 | 15 | 15 | 40 | 15 | 10 | 5 |
| 거래량 소진형 | 15 | 40 | 15 | 15 | 10 | 5 |
| 코인 상대강도형 | 20 | 20 | 20 | 15 | 5 | 20 |

### 3-4. 카드

- **배지**: 등급 대신 순위. 색은 기존 순차 램프 재사용 — `#1~3` seq-200, `#4~10` seq-300, `#11~30` seq-400, `#31+` seq-500.
- **2행**: `내 점수 88.4 · 기본 71.3 (▲38위)` — 기본 목록(items)에 없던 종목이면 `▲38위` 자리에 `NEW 발굴` 태그.
- **3행**: 내 비중 기준 기여도 상위 3개를 태그로. 기존 `.rev-tags` 스타일 재사용.
- **4행**: 하드 필터로 거른 항목의 실제 값(손익비·거래대금·경고).

### 3-5. 상세 모달

1. **점수 비교** — `내 점수` / `기본 점수` 막대 2개를 나란히.
2. **왜 내 설정에서 올라왔나** (핵심) — 지표별 기여도 델타를 문장으로 생성:
   > OBV(+8.4) · 거래량(+5.1) 비중을 올린 덕분에 **기본 61위 → 내 설정 12위**로 올라왔습니다.
   > 대신 이동평균 비중을 낮춰 −6.2점을 잃었습니다.

   계산: `Δ_k = s_k × (w_k/W − w0_k/100)`. 절대값 상위 2개(+), 하위 1개(−)를 문장화.
3. **지표별 기여도 막대** — 8개, `s_k × w_k / W` 내림차순. 회색 마커로 기본 기여도 위치 병기.
4. **기존 8지표 상세**(수치·판정문) — `items`에 있는 종목만. 없으면 안내:
   > 이 종목은 기본 점수가 55점 미만이라 지표별 수치 상세는 제공되지 않습니다. 위 점수 막대는 확인할 수 있습니다.

   (기존 `openSearchDetail`이 같은 패턴을 이미 쓰고 있다 — 톤을 맞춘다.)

---

## 4. 스코어링 사양

### 4-1. 매수신호 재계산 (검증 완료)

서버 `score.evaluate()`를 클라이언트에서 그대로 재현한다.

```
입력: row.s[tf][k]  — 종목의 tf(일봉/주봉/4시간봉)별 지표 k 원점수 (0~100)
      w[k]          — 사용자 비중 (슬라이더 원값, 0~50)
      tfw[tf]       — 타임프레임 비중 (kr/us: 1d .70, 1w .30 / coin: 4h .25, 1d .50, 1w .25)

W = Σ w[k]                                   ← 합계가 100이 아니어도 자동 정규화
if W == 0        → 계산 중지, 안내 문구
tf_total[tf] = Σ_k ( s[tf][k] × w[k] ) / W
norm         = Σ_tf tfw[tf]                  ← 데이터 부족으로 빠진 tf 제외 후 재정규화
combined     = Σ_tf tf_total[tf] × tfw[tf] / norm
if (tf 2개 이상) and (모든 tf_total ≥ 60)     → combined = min(100, combined + 5)
최종         = clamp(combined, 0, 100)
```

- `1d`가 없는 종목은 서버와 동일하게 후보에서 제외한다.
- 정렬 보너스(+5)까지 재현해야 기본 프리셋에서 매수신호 탭과 순서가 같아진다. 실측 최대 오차 **0.027점** (지표 점수를 소수 1자리로 반올림해 내려주는 데서 오는 오차뿐).

### 4-2. 추세전환 재계산

추세전환 카테고리 점수는 0~100이 아니라 **카테고리별 만점**(25/20/20/15/10/10) 기준의 절대 점수다. 먼저 100 기준으로 환산한 뒤 같은 공식을 쓴다.

```
ŝ[c]     = cat_score[c] / cat_max[c] × 100      ← 0~100 정규화
W        = Σ w[c]  (해당 종목에 존재하는 카테고리만 — 주식은 coin 제외)
최종      = Σ_c ( ŝ[c] × w[c] ) / W
단계 판정 = 최종 ≥ (사용자 최소 점수) 이면 후보,
           breakout_confirmed 이면 '확인 진입' 아니면 '초기 진입'
```

- 기존 `EARLY_MIN_SCORE = 35` 컷은 화면의 **최소 점수 슬라이더**가 그대로 대신한다. 새 컨트롤 불필요.
- 주식은 `coin` 카테고리가 없으므로 W에서 자동으로 빠진다(서버의 90점 만점 재환산과 같은 규칙).

### 4-3. 필수 조건 (하드 필터)

비중으로는 거를 수 없는 것들. 전부 AND 결합.

| 항목 | 선택지 | 기본값 | 필요 데이터 |
|---|---|---|---|
| 거래대금 최소 | 제한없음 / 1억 / 10억 / 50억 / 100억 (미국은 $1M/$10M/$50M/$100M) | 시장별 하한 | `turnover` |
| 손익비 최소 | 제한없음 / 1.5 / 2.0 / 2.5 / 3.0 | 제한없음 | `rr` |
| 경고 신호 | 제한없음 / 0건만 / 1건 이하 | 제한없음 | `warn` |
| 당일 등락률 상한 | 제한없음 / +10% / +15% / +20% | 제한없음 | `change_pct` |
| 하루 변동폭(ATR) 상한 | 제한없음 / 5% / 8% / 12% | 제한없음 | `atr_pct` |

필터로 제외된 건수는 타일에 **"필터 제외 41"**로 항상 노출한다. 조용히 사라지지 않게 한다(앱 전반의 원칙).

### 4-4. 등급 대신 순위

- 커스텀 점수에는 S/A/B/C를 붙이지 않는다.
- 대신 `#순위` 배지 + 카드 안 `기본 71.3` 병기.
- 가이드에도 명시: **"내 전략 탭의 점수는 내 비중으로 다시 계산한 값이라, 매수신호 탭의 등급 컷(85/75/65/55)과 같은 의미를 갖지 않습니다."**

---

## 5. 데이터 설계

### 5-1. 왜 새 파일인가

현재 `{market}_latest.json`은 **55점 이상 상위 200종목**만 `parts`(지표 상세)를 갖는다. 내 비중으로 상위권이 될 나머지 1,600종목에는 지표 점수가 아예 없다. 그렇다고 `latest.json`에 다 넣으면 매수신호 탭 첫 로딩까지 무거워진다.

→ **`docs/data/{market}_matrix.json`을 새로 만들고, 사용자가 "내 전략"을 처음 누를 때만 받는다.**

### 5-2. 스키마

```jsonc
{
  "market": "kr",
  "data_date": "2026-08-16",          // latest.json과 대조해 짝이 맞는지 확인
  "generated_at": "2026-08-16T16:40:12+09:00",
  "keys": ["ma","volume","structure","rsi","macd","bb","adx","obv"],
  "tfs": ["1d","1w"],
  "tf_weights": {"1d":0.70,"1w":0.30},
  "align": {"threshold":60,"bonus":5},
  "default_weights": {"ma":25,"volume":20,...},   // 기본 프리셋 = 서버 산식과 동일 보장
  "presets": { "추세 추종형": {...}, ... },        // 프리셋도 서버에서 내려 배포로 갱신 가능

  "row_fields": ["symbol","s1d","s1w","okmask","turnover_m","rr","atr_pct","warn"],
  "rows": [
    ["005930", [81.7,74.0,60.2,55.1,48.9,70.3,40.0,66.4],
               [59.1,61.4,50.0,44.2,40.8,55.6,35.1,58.0], 173, 312000, 2.84, 3.1, 0],
    ...
  ],

  // 추세전환 커스텀용 (Phase 3)
  "rev_keys": ["structure","volume","momentum","ma","volatility","coin"],
  "rev_max":  {"structure":25,"volume":20,"momentum":20,"ma":15,"volatility":10,"coin":10},
  "rev_row_fields": ["symbol","cats","decline_pct","breakout"],
  "rev_rows": [
    ["005930", [18.5,12.0,14.5,9.0,6.5,null], -32.4, 1],
    ...
  ]
}
```

설계 근거:

- **행을 객체가 아니라 배열로** — 키 이름 반복이 없어 크기가 절반이다. `row_fields`로 자기 문서화.
- **이름·가격·링크는 넣지 않는다** — `latest.json`의 `search_index`가 전 종목 기준으로 이미 갖고 있다. `symbol`로 조인. (약 50KB 절약)
- **`okmask`** — 8지표의 `ok` 플래그를 비트마스크 정수 하나로. 카드의 "충족 n개" 표시용. 비중 0인 지표는 세지 않는다.
- **`turnover_m`** — 백만 단위 정수(원/달러). 필터용이며 표시는 기존 `fmtM`을 재사용.
- **점수는 소수 1자리** — 정수로 반올림하면 국내 기준 200KB→142KB로 줄지만, 재계산 오차가 0.027점에서 약 0.5점으로 커져 기본 프리셋 = 매수신호 탭 순서 불변식이 흔들린다. **정확성을 택한다.**
- **`rev_rows`는 하락 15% 이상 전 종목** — 현재 `reversal_candidates`는 35점 컷을 이미 통과한 것만 담는다. 컷 자체가 사용자 비중에 따라 달라지므로, 매트릭스에는 **컷 적용 전** 전량을 담아야 한다.

### 5-3. 크기 예산 (실측 시뮬레이션)

| 시장 | 종목 수 | 원본 | gzip 전송 | 종목당 |
|---|--:|--:|--:|--:|
| 국내주식 | 1,800 | 200 KB | **77 KB** | 114 B |
| 미국주식 | 600 | 67 KB | **26 KB** | 114 B |
| 코인 | 180 | 20 KB | **8 KB** | 114 B |

GitHub Pages는 gzip을 자동 적용한다. 국내 기준 **77KB 한 번**이면 되고, 서비스워커가 캐시하므로 재방문 시 0. 지연 로드라 매수신호 탭만 쓰는 사용자에게는 영향이 없다.

### 5-4. 파이썬 변경점

**`screener/score.py`** — `evaluate()`가 타임프레임별 지표 점수를 함께 반환하도록 한 줄 추가.

```python
# 현재: tf_results[tf]["parts"]를 만들어놓고 1d만 쓰고 버린다
return {
    ...,
    # 추가 — 내 전략 탭 매트릭스용. 값은 이미 계산돼 있으므로 비용 0.
    "tf_scores": {tf: {k: v["score"] for k, v in r["parts"].items()}
                  for tf, r in tf_results.items()},
}
```

**`screener/run.py`** — `_payload()` 곁에 `_matrix()`를 추가하고, 각 `screen_*()`가 평가 성공한 **모든** 종목(현재 `search_index`에 넣는 시점)에서 행을 쌓는다. `reversal.evaluate_reversal()`은 이미 컷 이전 값을 갖고 있으므로 `stage is None`인 경우도 `rev_rows`에는 넣는다(`_add_reversal`에 인자 하나 추가).

**`screener/config.py`** — `WEIGHT_PRESETS`, `REV_WEIGHT_PRESETS`, `HARD_FILTER_OPTIONS` 추가. 프리셋을 서버 상수로 두면 배포만으로 조정 가능하고 파이썬 테스트로 합계 100을 검증할 수 있다.

**워크플로 변경 불필요** — `.github/workflows/screen-*.yml`이 `git add docs/data`(디렉터리 단위)라 새 파일이 자동으로 커밋된다.

---

## 6. 저장 · 공유 · 상태

### 6-1. 저장 (localStorage)

```jsonc
// key: "screener.strategies.v1"
{
  "active": "auto",                        // 마지막으로 쓰던 설정
  "auto":  { "base":"signal", "w":{...}, "filters":{...} },
  "slots": [
    { "id":"a1", "name":"내 조합 A", "memo":"거래량이 먼저 터진 종목을 먼저 본다",
      "base":"signal", "w":{...}, "filters":{...}, "updated":"2026-08-16" }
  ]
}
```

- 슬롯 최대 **5개** + 마지막 설정 자동 저장 1개.
- 저장 시 **메모 한 줄(선택)** 을 받는다. "왜 이 비중인가"를 스스로 적게 하는 장치 — 나중에 열었을 때 자기 논리를 다시 읽게 된다.
- 이 앱은 GitHub Pages 정적 사이트이고 CSP는 `connect-src 'self'`라 데이터가 밖으로 나가지 않는다. 저장은 전적으로 기기 안에서만 일어난다 — 가이드에 명시.

### 6-2. 공유 (URL 해시)

```
https://…/#s=1.signal.25-20-20-10-8-7-5-5.10-2.0-0-15-0
          └ 버전 └ 기준  └ 8지표 비중          └ 필터 5개
```

- 해시만 쓰므로 서버 왕복이 없고 CSP(`base-uri 'none'`, `form-action 'none'`)와 무관하다.
- 해시가 있으면 로드 직후 적용하고 상단에 안내: **"공유받은 전략을 불러왔습니다 · [내 설정으로 저장]"**.
- 버전 접두사(`1.`)로 향후 포맷 변경 시 무시할 수 있게 한다. 파싱 실패하면 조용히 기본값으로.

---

## 7. 성능

| 항목 | 실측/추정 | 판단 |
|---|---|---|
| 1,800종목 재계산 + 정렬 1회 | **0.71 ms** (Node, 데스크톱) | 슬라이더 드래그 중 매 이벤트 재계산해도 무해. 저사양 폰 5배 가정해도 3~4ms |
| 200KB JSON 파싱 | 약 10 ms | 최초 1회 |
| 카드 100장 `innerHTML` | 20~40 ms | **여기가 병목** |

대응:

- 계산은 즉시, **렌더는 `requestAnimationFrame` 1회로 합친다**(연속 input 이벤트를 한 프레임에 한 번만 그림).
- 카드는 상위 **100장**만 그린다. 그 아래는 "n건 더 있음 · 최소 점수를 올려 좁혀보세요" 안내(기존 `truncated` 문구 패턴과 동일).
- 매트릭스는 파싱 후 **typed 배열 대신 평범한 배열**로 둔다 — 이 규모에서는 차이가 없고 코드가 단순해진다.

---

## 8. 예외 · 엣지 케이스

| 상황 | 처리 |
|---|---|
| 비중 합계 0 (전부 0으로 내림) | 계산 중지. "지표를 최소 하나는 0보다 크게 두세요." 마지막 유효 결과는 유지 |
| 한 지표에 40% 초과 배정 | 결과는 정상 계산하되 노란 배지: "한 지표에 40% 이상을 몰면 그 지표가 흔들릴 때 결과 전체가 흔들립니다" |
| `matrix.json` 없음 (구버전 데이터) | "이 시장은 아직 내 전략 데이터가 없습니다. 다음 스크리닝 실행 후 사용할 수 있습니다." 토글은 살려두되 목록 자리에 안내 |
| `matrix.data_date ≠ latest.data_date` | 배포 타이밍이 어긋난 경우. "데이터 시각이 어긋나 잠시 후 다시 시도해 주세요" + 매트릭스 사용 중단 |
| 오프라인 | 서비스워커가 `/data/` 경로를 이미 네트워크 우선 → 캐시 폴백으로 처리. 매트릭스도 같은 규칙에 자동 포함 |
| 주봉 데이터 부족 종목 | 서버와 동일하게 tf 비중 재정규화. 일봉이 없으면 후보 제외 |
| 필터로 결과 0건 | "필수 조건이 너무 빡빡합니다. 제외된 41건 중 손익비 조건에서 33건이 걸렸습니다." — **어느 조건이 얼마나 걸렀는지** 알려준다 |
| 검색어 유지 | 콘텐츠 전환 시 검색어는 유지(현재 추세전환과 동일 동작) |

---

## 9. 과최적화 방지 — 제품 철학

이 기능은 잘못 쓰면 **"마음에 드는 종목이 1위가 될 때까지 슬라이더를 돌리는 기계"** 가 된다. 이 앱은 전체 톤이 "이건 필터일 뿐 매매 권유가 아니다"인데, 그 원칙을 이 탭에서 가장 세게 지켜야 한다.

장치 다섯 가지:

1. **기본 점수 상시 병기** — 모든 카드에 `기본 71.3`. 내 설정이 기본에서 얼마나 벗어났는지 항상 보인다.
2. **편중 경고** — 한 지표 40% 초과 시 노란 배지.
3. **등급 미부여** — 스스로 만든 S등급이 생기지 않는다(§4-4).
4. **저장 시 메모 유도** — "왜 이 비중인가"를 한 줄 적게 한다.
5. **가이드 문구** (고정 노출):
   > 슬라이더는 **결과를 보고 맞추는 게 아니라, 먼저 내 매매 논리를 정하고 그 논리를 숫자로 옮기는** 도구입니다. 결과가 마음에 들 때까지 비중을 돌리면, 그건 과거 데이터에 내 취향을 맞춘 것일 뿐 앞으로의 성과와는 무관합니다. 비중을 정했으면 최소 몇 주는 고정해두고 보세요.

---

## 10. 구현 계획

### Phase 0 — 데이터 기반 (UI 변경 없음, 단독 배포 가능)

- `score.py`: `tf_scores` 반환 추가
- `run.py`: `_matrix()` 생성 + `{market}_matrix.json` 기록, `_add_reversal()`이 컷 미달도 `rev_rows`에 남기도록 확장
- `config.py`: `WEIGHT_PRESETS` · `REV_WEIGHT_PRESETS` · `HARD_FILTER_OPTIONS`
- `run.py --market demo`: 매트릭스도 함께 생성(오프라인 미리보기)
- `tests/test_custom.py`: **불변식 테스트**(§11)
- 이 단계까지는 사용자 화면이 전혀 바뀌지 않는다. 안전하게 먼저 배포해 실제 파일 크기를 확인한다.

### Phase 1 — 매수신호 커스텀 UI

- 콘텐츠 토글 3버튼화 + 360px 미디어쿼리
- 매트릭스 지연 로드 + 상태(`state.matrix`, `state.weights`, `state.base`)
- 프리셋 7개, 접이식 슬라이더 8개, 합계 표시, 되돌리기
- 타일 5종 · 정렬 옵션(`내 점수순 / 기본 대비 상승폭 / 거래대금 / 등락률 / 손익비`)
- 순위 배지 카드 + 기여도 태그
- 상세 모달(점수 비교 · 왜 올라왔나 · 기여도 막대 · 기존 지표 상세 조건부)
- **`docs/sw.js`의 `CACHE`를 `screener-v7`로 올린다** — §13 참조(중요)
- 가이드 섹션 추가

### Phase 2 — 필수 조건 · 저장 · 공유

- 필수 조건 패널 5종 + "어느 조건이 몇 건 걸렀는지" 리포트
- localStorage 슬롯 5개 + 자동 저장 + 메모
- URL 해시 공유 · 복원
- `NEW 발굴` 배지, 순위 변화(`▲38위`) 계산

### Phase 3 — 추세전환 커스텀

- `rev_rows` 소비, 기준 라디오(매수신호 ↔ 추세전환)
- 6카테고리 슬라이더 + 프리셋 5종
- 최소 점수 슬라이더가 단계 컷 역할(별도 컨트롤 없음)

### Phase 4 — (다음 기획) 간이 백테스트

현재 `docs/data/history/{market}_{date}.json`은 **상위 60종목 · 일부 필드**만 보관해 재계산이 불가능하다. 하려면:

1. 히스토리에도 매트릭스를 얹는다(날짜당 국내 200KB → 1년 약 50MB. **압축 또는 상위 300종목 제한 필요**)
2. 또는 훨씬 가볍게: 날짜별 `{symbol: [8지표 점수]}`만 정수로 보관(날짜당 약 40KB)
3. 산출: "내 비중 상위 10종목의 20영업일 후 평균 수익률 vs 기본 비중 상위 10종목"

**주의**: 이 숫자는 그 자체로 과최적화를 유발한다. 넣는다면 반드시 "표본이 n일치뿐" 경고와 함께.

### 파일별 변경 요약

| 파일 | Phase | 변경 |
|---|---|---|
| `screener/score.py` | 0 | `tf_scores` 반환 (+4줄) |
| `screener/run.py` | 0 | `_matrix()` 신규, `screen_*` 3곳에서 행 수집, 파일 기록 (+70줄) |
| `screener/config.py` | 0 | 프리셋·필터 상수 (+45줄) |
| `tests/test_custom.py` | 0 | 신규 (약 120줄) |
| `docs/index.html` | 1~3 | CSS·상태·재계산·프리셋·슬라이더·카드·상세·저장·공유·가이드 (+450줄 예상) |
| `docs/sw.js` | 1 | `CACHE` 버전 상승 (1줄, **필수**) |
| `README.md` | 1 | "내 전략" 섹션 |

---

## 11. 테스트 계획

### 파이썬 (`tests/test_custom.py`)

1. **불변식(최우선)** — 매트릭스 행을 기본 가중치로 재계산한 값이 `items[i].score`와 **±0.1 이내**. 정렬 보너스가 붙은 종목을 반드시 포함. *(기획 단계에서 40종목 선검증 완료: 최대 오차 0.027)*
2. 매트릭스 행 수 == 평가 성공 종목 수 == `search_index` 길이
3. 모든 지표 점수가 0~100, `okmask`가 0~255
4. 주봉 부족 종목의 tf 재정규화가 서버와 동일
5. `rev_rows`가 "하락 15% 이상" 전 종목을 담는다 — 기본 35점 컷 미만 종목이 실제로 포함되는지
6. 모든 프리셋 합계 == 100, 키 집합이 `WEIGHTS`와 동일
7. `--market demo`가 매트릭스 파일까지 생성

### E2E (Playwright, 기존 패턴 유지)

1. 토글 3버튼 상태 전환, 시장 탭 이동 후에도 유지
2. **프리셋 "기본" 선택 시 상위 10종목의 순서가 매수신호 탭과 동일** (불변식의 UI 버전)
3. 프리셋 전환 → 순위가 실제로 바뀜
4. 슬라이더 조작 → 합계 표시 갱신, 목록 재정렬
5. 비중 전부 0 → 안내 문구, JS 에러 없음
6. 하드 필터 적용 → "필터 제외 n" 타일 증가, 제외 사유 리포트
7. 저장 → 새로고침 → 복원
8. URL 해시로 진입 → 비중 복원 + "공유받은 전략" 안내
9. `matrix.json` 404 상황(네트워크 인터셉트) → 안내 문구, 다른 탭 정상
10. 슬라이더 60회 연속 드래그 → 콘솔 에러 0, 렌더 누락 없음
11. 상세 모달 — `items`에 있는 종목 / 매트릭스에만 있는 종목 두 경로 모두

---

## 12. 리스크

| 리스크 | 영향 | 대응 |
|---|---|---|
| **서비스워커 캐시** — `index.html`이 `SHELL`에 있어 **캐시 우선**이다. `CACHE` 상수를 올리지 않으면 기존 사용자에게 새 탭이 **영원히 안 보인다** | 치명 | Phase 1에서 `screener-v7`로 상승. **덧붙여: 현재 `CACHE = "screener-v6"`의 주석이 "미국주식 탭"이라, 추세전환 배포 때 버전을 올렸는지 확인이 필요하다.** 안 올렸다면 기존 사용자는 추세전환 탭도 못 보고 있다 |
| 파일 크기 (국내 200KB) | 중 | 지연 로드 + gzip 77KB + SW 캐시. Phase 0 배포 후 실측해 초과 시 상위 1,000종목 제한 검토 |
| 매트릭스에만 있는 종목의 지표 상세 부재 | 중 | v1은 점수 막대만 + 안내 문구. 실측 후 `MIN_OUTPUT_SCORE`를 55→45로 낮추는 방안 검토(파일 +약 50%) |
| 과최적화 유도 | 중 | §9의 장치 5종 |
| 등급 오해 | 중 | 순위 배지 + 기본 점수 병기 + 가이드 명시 |
| `index.html` 비대화 (현재 1,000줄 → 1,450줄 예상) | 하 | 이 앱은 단일 파일 원칙(CSP·오프라인 단순성)을 지켜왔다. 유지하되 섹션 주석으로 구분. 1,800줄을 넘기면 그때 분리 검토 |

---

## 13. 확인이 필요한 사항

1. **국내 유니버스 실제 규모** — 시총 500억 + 거래대금 1억 필터 후 몇 종목인지 실행 로그로 확인해야 크기 예산이 확정된다(1,800종목으로 가정 중).
2. **`sw.js` 캐시 버전** — 추세전환 배포 시 올렸는지. 안 올렸다면 이번에 함께 바로잡아야 한다.
3. **프리셋 이름** — "눌림목형", "수급 중심형" 같은 용어가 초보자에게 통하는지. 필요하면 부제를 붙인다(예: "수급 중심형 — 거래량이 먼저 터진 종목").
4. **필터 기본값** — 처음 열었을 때 필터를 전부 "제한 없음"으로 둘지, 거래대금만 시장 하한을 걸어둘지.

---

## 14. 완료 기준 (Definition of Done)

- [x] 프리셋 "기본"에서 내 전략 탭 상위 10 == 매수신호 탭 상위 10 (순서까지)
      — `test_default_weights_reproduce_official_ranking` + E2E 3번 항목
- [x] 슬라이더 조작 시 체감 지연 없음 — 23ms/틱 (드래그 중 렌더 축소)
- [x] 매트릭스 없이도 기존 두 탭이 정상 동작 — `has_matrix` 표식 + 404 방어 (E2E 13번)
- [x] 오프라인에서 마지막 매트릭스로 내 전략 탭 사용 가능 — 서비스워커가
      `/data/` 경로를 네트워크 우선·캐시 폴백으로 이미 처리
- [x] 저장한 설정이 앱 재시작 후 복원 — `my_strategy_save.py` 1번 항목
- [x] 파이썬 93건 + E2E 6종 전부 통과 (기존 73건 회귀 포함)
- [x] 가이드에 내 전략 설명 · 과최적화 경고 · 등급 미부여 사유 수록

배포 전 마지막 확인:

- [ ] `python -m screener.run --market kr` 로 실제 유니버스 규모와 매트릭스 크기 확인
- [ ] 배포 후 폰에서 앱을 한 번 완전히 닫았다 열어 새 탭이 보이는지 확인
      (`sw.js` 캐시 버전 상승이 실제로 먹었는지)
